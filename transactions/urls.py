# from django.urls import path
# from . import views

# urlpatterns = [
#     path('', views.TransactionHistoryView.as_view(), name='transaction-history'),
#     path('<str:reference>/', views.TransactionDetailView.as_view(), name='transaction-detail'),
#     path('stats/', views.TransactionStatsView.as_view(), name='transaction-stats'),
# ]